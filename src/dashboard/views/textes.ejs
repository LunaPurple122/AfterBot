<%
    const safeTexts = typeof texts !== 'undefined' && texts ? texts : {};
%>
<section class="panel">
    <h1>Textes</h1>
    <label>Reglement<textarea readonly rows="10"><%= safeTexts.texte_reglement || 'Non configure' %></textarea></label>
    <label>Message alerte tickets<textarea readonly rows="6"><%= safeTexts.ticket_alert_message || 'Non configure' %></textarea></label>
</section>
