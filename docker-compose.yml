services:

  afterbot:

    container_name: afterbot

    build: .

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - .:/app

    command: node src/index.js

    networks:
      - postgres_default


networks:

  postgres_default:
    external: true